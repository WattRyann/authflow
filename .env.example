# JWT配置 (至少32字符)
JWT_ACCESS_SECRET=test_access_secret_key_must_be_at_least_32_chars_long
JWT_REFRESH_SECRET=test_refresh_secret_key_must_be_32_chars
ALGORITHM=HS256

# 邮件配置 (注意数值类型)
MAIL_USERNAME=test@example.com
MAIL_PASSWORD=test_password
MAIL_FROM=test@example.com
MAIL_PORT=587
MAIL_SERVER=smtp.example.com
MAIL_SSL_TLS=false
MAIL_STARTTLS=true
MAIL_USE_CREDENTIALS=true

# 验证码配置 (注意数值类型)
VERIFICATION_CODE_LENGTH=6
VERIFICATION_CODE_EXPIRE_MINUTES=10

# Redis配置 (注意数值类型)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0
REDIS_URL=redis://:${REDIS_PASSWORD}@${REDIS_HOST}:${REDIS_PORT}/${REDIS_DB}

# OAuth配置
GOOGLE_CLIENT_ID=test_google_client_id
GOOGLE_CLIENT_SECRET=test_google_client_secret
GITHUB_CLIENT_ID=test_github_client_id
GITHUB_CLIENT_SECRET=test_github_client_secret

# 应用配置
BASE_URL=https://vercel.com/wattryanns-projects/authflow
ALLOWED_ORIGINS=https://vercel.com/wattryanns-projects/authflow

DATABASE_URL="postgresql://johndoe:randompassword@localhost:5432/mydb?schema=public"
